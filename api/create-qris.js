module.exports = (req, res) => {
  res.status(200).json({
    ok: true,
    msg: "CREATE QRIS FUNCTION ALIVE"
  });
};};      message:
        'Server error: ' + (err && err.message ? err.message : 'Unknown error')
    });
  }
};
